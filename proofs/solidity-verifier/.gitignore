# Foundry build output
out/
cache/
broadcast/

# Runtime-generated fixtures (created by `cargo run --bin generate`).
# Per-circuit subdirectories (see src/circuits/ and contracts/circuits/).
fixtures/**/*.bin
fixtures/**/*.be
fixtures/**/*_trace.json

# Rust target
target/
